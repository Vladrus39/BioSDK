class FeedbackController:
    """Placeholder for reward/error/gain control.

    v0.1 logs feedback concepts but does not apply biological protocols.
    """
    def apply(self, result):
        return {"feedback": "none", "reason": "v0.1 placeholder"}
