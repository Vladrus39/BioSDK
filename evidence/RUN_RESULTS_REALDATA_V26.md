# RUN RESULTS — BioGPU v2.6 Encoder Layer

## Scope

v2.6 adds the encoder layer:

- spatial encoder;
- temporal encoder;
- rate encoder;
- hybrid encoder;
- encoder registry;
- demo pattern generation.

No live output was performed.

## Safety boundary

The encoder produces abstract patterns only and rejects unsafe live fields such as voltage, amplitude, pulse width, frequency, pinout and wiring.
