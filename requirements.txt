# Mirrors [project.dependencies] in pyproject.toml — keep in sync.
# For dashboard add: pip install ".[dashboard]"
# For dev tooling add: pip install ".[dev]"
# For everything:    pip install ".[all]"

numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
pydantic>=2.0
PyYAML>=6.0
h5py>=3.8
matplotlib>=3.7
