# BioGPU-Core Master Project Plan v4.9

## Current identity

**BioGPU-Core** remains the technical kernel and repository name.

The project is now positioned as:

```text
BioGPU-Core → BioSDK → BioCompute Runtime → NSI-1.0 → BioCompute Control Plane → BiC OS
```

## Near-term product

**BioCompute Runtime / BioSDK** for replay/read-only living neural compute workflows.

## Future OS goal

**BiC OS** — working codename for a future BioCompute OS / Biological Interface Compute OS.

## What we have now

```text
real-data evidence pack
pulse feature matrix
readout benchmarks
lineage-strict split
sklearn readouts
safety boundary
external API skeleton
BioLLM tool interface
enterprise packaging
beta server scaffold
clean PC runner patch
NSI-1.0 draft
```

## What remains before power-PC validation

v4.9 completes naming and OS roadmap. Next practical work is on the powerful PC:

```text
PC0 smoke/current tests
PC1 import full Pre_processed_MEA_data.zip
PC2 compact v33/v36 reproduction
PC3 full_shuffle_1000
PC4 extended_methods_5000
PC5 raw HDF5 / TTL reconstruction
PC6 DANDI / NWB parser
PC7 AllenSDK orientation benchmark
PC8 latency / energy measurement
PC9 final PC evidence bundle
```

## Claim boundary

Do not claim first biological computer or GPU replacement.

Allowed claim:

```text
BioGPU-Core is a vendor-neutral BioCompute Runtime / BioSDK and NSI draft for living neural compute, designed to evolve toward BiC OS.
```
