# BioGPU v2.1 Result Bundle

The result bundle exporter packages:

- `session.json`
- selected output folders/files
- `manifest.json`
- generated reports

This is intended for the workflow where heavy runs are executed on a stronger PC and the output bundle is returned for analysis and report writing.
