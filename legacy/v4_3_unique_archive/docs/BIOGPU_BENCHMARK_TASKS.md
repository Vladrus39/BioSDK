# BioGPU Benchmark Tasks

B0 target-vs-random electrode — implemented on replay data.

B1 spot localization — exploratory, needs balanced protocol.

B2 temporal pattern classification — candidate first live BioGPU benchmark.

B3 orientation-like encoding — future Allen/live benchmark.

B4 adaptive closed-loop task — key long-term route toward BioGPU advantage.
